// Actions

// Reducer
const initial = {
    currentPosition: "24-42",
    direction: "left"
}

export default function reducer(state = initial, action = {}) {
    switch (action.type) {
        default:
            return state
    }
}

// Action Creator